import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('ViewCompanyPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ViewCompanyPage(title: 'ViewCompany')));
    //  final titleFinder = find.text('ViewCompany');
    //  expect(titleFinder, findsOneWidget);
  });
}
